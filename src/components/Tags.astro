---
const { tags } = Astro.props;
---

<div class="tags-container">
    {tags?.map(tag => 
        <small>
            <a class="tag-links" href=`/tags/${tag}`>{tag}</a>
        </small>
    )}
</div>